# 豆包语音助手 Demo 依赖清单
# Python 版本要求: >= 3.8

# ==================== 图形界面 ====================
PyQt6>=6.5.0

# ==================== 网络通信 ====================
requests>=2.31.0
websockets>=11.0.3

# ==================== 音频处理 ====================
# pyaudio: 麦克风录音
# Windows: pip install pyaudio
# macOS: brew install portaudio && pip install pyaudio
# Linux: sudo apt-get install portaudio19-dev && pip install pyaudio
pyaudio>=0.2.13

# pygame: 音频播放
pygame>=2.5.2

# ==================== 计算机视觉 ====================
# OpenCV: 摄像头拍照功能
opencv-python>=4.8.0

# ==================== 人脸识别 ====================
# face_recognition 依赖 dlib
# Windows 安装方式（推荐）:
#   pip install dlib-bin
#   pip install face_recognition
# macOS/Linux:
#   brew install cmake  (macOS)
#   pip install face_recognition
face_recognition>=1.3.0
numpy>=1.24.0

# ==================== 物体检测 ====================
# YOLOv8 物体检测
# 首次运行会自动下载模型（yolov8n.pt 约 6MB）
ultralytics>=8.0.0

# ==================== 声纹识别 ====================
# Resemblyzer: 基于 d-vector 的说话人识别
# 特点: 轻量级，可在 CPU 上运行
# 依赖: torch, librosa, webrtcvad
resemblyzer>=0.1.4

# ==================== 记忆服务 ====================
# Mem0: 智能记忆系统（使用 HTTP API 调用，无需额外依赖）
# 服务地址配置在 config.py 中的 MEM0_BASE_URL
# 功能: 存储用户喜好、关系、重要事实等长期记忆

# ==================== 可选依赖 ====================
# python-dotenv>=1.0.0  # 如需从 .env 文件加载环境变量
